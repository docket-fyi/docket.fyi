{
  "scripts": {
    "examples": "$(npm bin)/docusaurus-examples",
    "start": "$(npm bin)/docusaurus-start",
    "build": "$(npm bin)/docusaurus-build",
    "publish-gh-pages": "$(npm bin)/docusaurus-publish",
    "write-translations": "$(npm bin)/docusaurus-write-translations",
    "version": "$(npm bin)/docusaurus-version",
    "rename-version": "$(npm bin)/docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.14.0"
  }
}
